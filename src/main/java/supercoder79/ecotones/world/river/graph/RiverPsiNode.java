package supercoder79.ecotones.world.river.graph;

public class RiverPsiNode extends RiverNode {
    public RiverPsiNode(double x, double z, double radius, double depth, double angle) {
        super(x, z, radius, depth, angle);
    }
}
